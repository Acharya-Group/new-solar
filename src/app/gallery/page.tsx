import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Solar Installation Gallery – Before & After | Neo Solar",
  description: "View real solar panel installations by Neo Solar across India.",
};

export default function GalleryPage() {
  return (
    <main className="min-h-screen">
      {/* TODO: Add Gallery page content */}
    </main>
  );
}
